import { useMutation, useQueryClient } from '@tanstack/react-query';
import { {{camelCase feature}}Api } from '../api';

export function use{{pascalCase action}}{{pascalCase feature}}() {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: {{camelCase feature}}Api.{{camelCase action}},
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['{{kebabCase feature}}'] });
    },
  });
}
